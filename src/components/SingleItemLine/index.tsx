import React, {FC} from 'react';
import {Item} from "../../utils/types";

interface IProps{
    item: Item;
}

const SingleItemLine: FC<IProps> = ({item}) => {
    return (
        <div>

        </div>
    );
};

export default SingleItemLine;
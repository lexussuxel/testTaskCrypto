import {FC} from 'react';
import { WrapperComponents } from '../../UI';

const About: FC = () => {
    return (
        <WrapperComponents>
            about
        </WrapperComponents>
    );
};

export default About;